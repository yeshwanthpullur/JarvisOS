# No runtime dependencies are required for the current local-first runtime.
# Python 3.12+ standard library modules provide configuration, logging, paths,
# and the localhost desktop interface service.
