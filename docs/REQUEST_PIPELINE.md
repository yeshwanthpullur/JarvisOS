# Request Pipeline

The request pipeline normalizes input, builds conversation requests, builds context metadata, resolves intent through Executive JARVIS, plans execution, dispatches, composes a response, and records history.

