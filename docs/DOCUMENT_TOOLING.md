# Document Tooling

Docling and Marker are optional isolated document adapters. Plain-text parsing remains the built-in fallback. File access must be explicit and bounded; extracted private content is not persisted by default. OCR, bulk ingestion, and external parsing remain unavailable until configured and policy-approved.
