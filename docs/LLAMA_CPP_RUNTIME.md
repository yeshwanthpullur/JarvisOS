# llama.cpp Runtime

llama.cpp is optional and represented independently from GGUF model artifacts. Detection recognizes common local binary forms without downloading or building them. Local server endpoints must use loopback; model compatibility, quantization, memory, context, and health remain separately verified metadata.
