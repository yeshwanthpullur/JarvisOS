# High Availability

Prompt 83 provides high-availability planning primitives: service/dependency registration, heartbeats as metadata, health aggregation, bounded circuit breakers, fault isolation, load-shedding advice, failover planning, and capacity estimates. It does not implement distributed clustering, leader election, replication, or automatic infrastructure changes.
