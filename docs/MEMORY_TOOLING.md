# Memory Tooling

Qdrant, ChromaDB, FAISS, Mem0, and Graphiti are optional isolated adapters. Persistent Memory remains authoritative. Detected packages cannot silently ingest, rewrite, delete, synchronize, or expose memory. Lexical retrieval remains the safe fallback when vector or graph backends are unavailable.
