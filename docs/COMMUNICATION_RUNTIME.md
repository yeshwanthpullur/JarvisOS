# Communication Runtime

Communication connectors remain disabled and unconfigured by default. The Phase 6 connector registry reports metadata but does not poll, send, schedule, attach files, enumerate contacts, or expose credentials. Any supported future send must use a validated destination, data-egress policy, exact approval, provider-specific rate limits, and Broker dispatch.
