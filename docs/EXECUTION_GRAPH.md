# Execution Graph

The execution graph describes how workflow steps relate to each other.

Supported graph concepts:
- directed graph
- sequential paths
- parallel paths
- conditional paths
- nested paths
- validation
- optimization
- future DAG execution

The graph is metadata only and exists to support future workflow orchestration.
