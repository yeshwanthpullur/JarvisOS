# Knowledge Provenance

Retrieval preserves `source_id -> record_id -> chunk_id`, locator, trust class, privacy class, freshness, and ranking warnings. Research candidates are eligible only when provenance exists and still require explicit controlled admission. Manual knowledge is labelled user-provided, not externally verified.
