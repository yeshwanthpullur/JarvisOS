# Runtime Diagnostics

Diagnostics provide bounded health, dependency, provider, resource, warning, error, recommendation, root-cause, and correlation metadata. Root causes are conservative categories such as configuration, dependency, provider, resource, policy, approval, Broker, or unexpected.

`runtime diagnostics`, `alerts`, `events`, `dashboard`, `metrics`, and `verify` never include credentials, private paths, payloads, or raw conversations.
