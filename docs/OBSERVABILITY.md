# Observability

Phase 6 records bounded in-memory metric points and trace metadata. Domains and labels are allowlisted; prompts, responses, transcripts, audio, document text, memory content, credentials, private paths, and connector payloads are excluded. Retention is bounded and telemetry upload is disabled.

Observability is informational only. It cannot execute recovery, change policy, approve work, or dispatch tools.
