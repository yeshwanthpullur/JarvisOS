# Performance

Phase 6 exposes measured-versus-estimated metric metadata for startup, providers, models, memory, and voice. Profiling is disabled by default and must remain bounded when enabled. Missing metrics are reported as unavailable rather than invented.

Current automated validation measures bounded command/runtime latency. Real model, browser, OCR, STT, and TTS latency requires the corresponding local runtime or hardware and is reported separately.
