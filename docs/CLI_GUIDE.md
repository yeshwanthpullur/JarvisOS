# CLI Guide

The interactive `Jarvis >` prompt is backed by the Conversation and Command engines. Supported built-ins include `help`, `status`, `health`, `version`, `history`, `metrics`, provider/plugin/agent/department summaries, and `exit`.

