# Workflow Scheduler

The Workflow Scheduler prepares workflow dispatch timing and queue metadata.

Supported modes:
- immediate
- scheduled
- queued
- priority
- dependency aware
- retry queue
- recovery queue
- future cron scheduling

The scheduler is a planning component only.
