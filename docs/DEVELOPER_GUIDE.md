# Executive Developer Guide

To extend Executive JARVIS:

1. Add focused typed models.
2. Inject dependencies through `JarvisContext`.
3. Register new components in `JarvisRegistry`.
4. Keep provider work behind Provider Router.
5. Keep specialized agents behind Agent Creator.
6. Add tests for lifecycle, health, and compatibility.

