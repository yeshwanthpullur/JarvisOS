# Core Agents

JARVIS remains the Executive. Core agents are permanent interfaces owned by the Agent Framework. Specialized agents must be generated through Agent Creator rather than hardcoded in JARVIS.

Current core agent examples live under `agents/examples/` and inherit `BaseAgent`.

