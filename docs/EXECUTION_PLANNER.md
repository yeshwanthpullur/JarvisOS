# Execution Planner

The execution planner is architecture-only. It prepares direct, memory, knowledge, task, plugin, provider, agent, workflow, hybrid, fallback, and recovery plan metadata without executing work.

