# Adapter Agent

Adapter and MCP schemas may inform model reasoning, but model output cannot invoke tools directly.

Adapter Agent may coordinate plugin, provider, and MCP resolution, but cannot grant trust, permission, credentials, or execution authority.

Adapter Agent coordinates MCP server resolution, discovery, trust-aware capability mapping, resource planning, and tool-call planning. It cannot grant trust, permission, approval, or execution and cannot install servers.
