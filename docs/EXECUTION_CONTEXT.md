# Execution Context

Shared execution context is temporary and request-scoped. Named segments cover conversation, research, knowledge, documents, browser, GitHub, MCP, plugins, model routing, provider metadata, and execution metadata. Updates create bounded immutable versions with author and reason. Context expires with its session and is never Persistent Memory.
