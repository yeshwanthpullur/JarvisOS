# vLLM Runtime

vLLM is optional. Discovery reports whether a runtime executable is present; metadata registration does not mean a server or model is ready. Local endpoints must bind to loopback. Heavy startup requires a resource plan, explicit approval, and Broker-controlled future executor; Prompt 78 performs no automatic startup.
