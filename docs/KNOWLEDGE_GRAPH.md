# Knowledge Graph

Graphiti is the planned temporal relationship adapter. It is optional and unconfigured. Graph writes require Memory Intelligence authority and provenance. New imports do not override newer verified facts merely because they were imported later; explicit corrections and authoritative runtime facts have higher precedence.

When Graphiti is unavailable, ordinary chat and semantic/lexical retrieval continue in a degraded state. No relationship is silently created or deleted.
