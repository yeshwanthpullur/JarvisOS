# Workflow Simulation

Simulation performs deterministic planning, validation, dependency analysis, resource estimation, approval estimation, and risk reporting. It never runs steps, contacts providers, changes files, mutates memory, sends communications, or creates external side effects.

Use `workflow simulate <request>` for a bounded policy summary. Simulation output states that policy, approval, and Broker authority are still required.
